# セッション記録

## 会話の要約
- rin.musicリポジトリの作詞チームエージェントを新しく設定した
- 既存の `lyrics_team.py` と `app.py` を一度全削除し、ゼロから作り直した
- セッション保存・読み込みの合言葉を新たに決定した
  - 保存：「マイクオフ」
  - 読み込み：「セッションスタート」
- CLAUDE.md を作成し、GitHubのMCPツールで会話を保存・読み込みする記憶プロトコルを導入した
- app.py の Gradio バージョン互換性エラー（`bubble_full_width`・`theme` 引数）を修正した
- セッション起動フック（`.claude/hooks/session-start.sh`）を追加し、依存関係を自動インストールする設定をした
- `.gitignore` に `__pycache__/` と `sessions/` を追加した

## 決定事項
- 保存の合言葉：**マイクオフ**
- 読み込みの合言葉：**セッションスタート**
- 記憶方式：GitHubの `memory/session.md` にMCPツールで保存・読み込み
- チーム構成：yasu（CEO）／龍姫（テーマ）／レイ（作詞）／ルキ（レビュー）

## 次回への引き継ぎ
- `app.py` のブラウザ起動確認がまだできていない（リモート環境のため localhost に繋がらない問題）
- 別リポジトリ（rin.business）の設定を参考に記憶プロトコルを導入済み
- 次回は `app.py` の動作確認、または追加機能の相談から始める
